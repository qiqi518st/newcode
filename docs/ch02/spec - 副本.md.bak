# NewCode TUI 多轮对话 — 需求规格 (spec.md)

## 背景

NewCode 是一个终端 AI 编程助手（类似 Claude Code），

第一版聚焦纯对话体验，打通"人 ↔ LLM"的最小闭环, 不做工具调用、文件操作、记忆等 Agent 能力。让用户能在终端里与大模型进行流畅的多轮对话。

使用 Python 实现。ch02 是其 TUI 版本，提供 Claude Code 风格的终端交互体验：用户可以在终端中与 AI 进行持续多轮对话，AI 流式回复并渲染 Markdown。

```
强调先打通人和llm的多轮对话循环，
样例文档中没有提具体的实现技术栈 
```



## 目标

- 用户在终端获得接近 Claude Code 的对话体验
- 支持 Anthropic 和 OpenAI 两种 API 后端，通过 YAML 配置切换
- Provider 层抽象为统一接口，方便后续扩展新后端
- 流式输出使用 SSE 协议，支持 Claude extended thinking



```

示例文档“打通 LLM API 调用：能向大模型发起请求并正确接收回复”和我的 “用户在终端获得接近 Claude Code 的对话体验” 
效果应该是类似的

新增： 也可以勉强说已经包含在获得类似claude code的对话体验中，先不加试试看 

提供一个全功能终端界面（TUI），承载输入、流式输出与多轮对话
回复以流式方式实时呈现，结束后以 markdown 形式美化展示。
在单次会话内维护完整对话上下文，支持连续多轮交流。
对调用失败有可恢复的错误反馈，不中断会话。
```



## 功能需求

- F1: **TUI 多轮对话** — 启动后进入 Claude Code 风格 REPL，输入输出交织，流式逐字打印，`> ` 提示符, 该轮回复结束后，将其整段以 markdown  形式重新渲染美化（代码块、列表、强调等）后定型展示。

- F2: **单次调用模式** — `newcode -c "问题"` 直接输出回复后退出，不进入 TUI

- F3: **流式输出 ** — 以流式方式接收回复，实时解析出正文文本增量并向界面输送。对扩展思考产生的思考  增量正确识别但不渲染（接收即丢弃），不得混入正文。

- F4: **滑动窗口上下文** — 保留最近 N 轮对话（默认 20），超出自动丢弃最早消息对

- F5: **多行输入检测** — 用户在输入框键入文本，可用 Alt+Enter 插入换行进行多行编辑；按 Enter 提交。提交后清空输入框，界面进入等待/流式状态，期间不接受新的提交，直至本轮回复结束。 换行的快捷键最好能在输入框中进行提示 

- F6: **多种退出方式** — `/exit`、`/quit`、`Ctrl+C`、`Ctrl+D` 四种方式均可退出

- F7: **System Prompt 可定制** — 默认内置 system prompt，用户可通过配置文件覆盖

- F8: **YAML 配置管理** — 配置文件 `~/.newcode/config.yaml`，不存在则提示创建并退出

- F9: **双 Provider 支持** — 支持 Anthropic 协议和 OpenAI 协议，通过配置切换

- F10: **Provider 抽象接口** — 统一的 LLM 调用接口，封装不同 API 协议差异，方便扩展新 provider

- F11: **Extended Thinking 支持** — 支持 Claude extended thinking，通过配置 `thinking: true` 启用

- F12: **Markdown 渲染** — AI 回复中的 Markdown 通过 Rich 库渲染后显示

- F13: **错误反馈** — 当请求失败（鉴权失败、限流、网络中断、模型不存在等）时显示错误信息，以可区分的样式展示, 程序不退出，保留对话历史，用户可重试

-  F14: **发起对话请求**  — 将"内置系统提示词 + 当前完整对话历史"作为上下文，向活动 provider 发起一次对话  请求，并按配置决定是否开启扩展思考。

- F15: **终端界面布局**  — 启动后呈现一个全功能终端界面，自上而下包含： 

  (a) 启动横幅：ASCII 小狗图案 + 应用名与版本号 + 当前工作目录；

  (b) 一行就绪提示信息（取代参考图中与 MCP/工具相关的状态行）；

  (c) 对话区：依时间顺序展示历次用户输入与助手回复；  

  (d) 底部带边框的输入框，含 ❯ 提示符与占位文字（如 "Send a message..."）； 

  (e) 底部状态栏：左侧显示活动 provider 的名称，右侧显示其模型名。

- F16: **响应计时**  —  自请求发出（开始等待模型）即启动计时，以"进行中"指示实时显示已用秒数  （形如 "Imagining… (5s)"，秒数随时间递增）；收到首个增量后继续计时；本轮回复  结束后定型显示该轮总耗时。

- F17: **流式中断**：ESC取消正在进行的回复

- 自动重试 / 限流退避：出错先自动重试, 重试次数3, 重试间隔3秒, 界面中需要用可区分的样式展示重试次数



```
 <!-- @ignore
这个就先不要了， 感觉这个功能作用不算太大
F2: provider 选择
  若配置中仅有一个 provider，直接采用它进入对话；若有多个，启动后先呈现一个选择
  界面（方向键列表，列出各 provider 的名称与模型），用户选定一项后再进入对话。
  被选定者即本次会话的活动 provider。
  
新增  默认提示词F7有提到，是否发送上下文以及是否开启思考之前没有说明
F14: **发起对话请求**  — 将"内置系统提示词 + 当前完整对话历史"作为上下文，向活动 provider 发起一次对话  请求，并按配置决定是否开启扩展思考。

F3 需要修改 
spec应该聚焦行为而非实现 — "SSE 协议"是实现细节，spec 应该描述"实时解析正文增量"这个行为
覆盖了关键边界情况 — 扩展思考的增量必须正确识别并丢弃，不能混入正文。这是容易出 bug 的地方

F4 比模板更好
模板中的"退出后不保留"有价值但位置不对 — 这个是 Out of Scope 的内容（对话历史不持久化），不应该混在功能需求描述里。F4
  没提这个是对的，它已经在"不做的事"里覆盖了。
模板(F6) 有误导性 — "完整对话历史"暗示无上限保留，但实际行为是 capped 的（默认 20 轮）。如果实现者按 F6 去理解，可能会做成无限增长的上下文，和实际需求矛盾

F11 的功能描述
Extended Thinking 是 Claude 模型的一个能力，不是显示选项。(类似deepseek的深度思考)
启用后，Claude 在回答复杂问题时会先"内部思考"再给出答案，回复质量更高（类似让模型多想一会儿）。用户不需要看到思考过程
用户端看到的效果就是：同样的配置，开 thinking: true 后回复更靠谱，但界面上没任何区别，看不到思考过程

F15 界面相关的内容确实有漏了, 补上了

F1 补上一句
该轮回复结束后，将其整段以 markdown
  形式重新渲染美化（代码块、列表、强调等）后定型展示。
 
F5 修改 比原版 更详细 
用户在输入框键入文本，可用 Alt+Enter 插入换行进行多行编辑；按 Enter 提交。提交后
  清空输入框，界面进入等待/流式状态，期间不接受新的提交，直至本轮回复结束。

F5的原文  自动检测未闭合括号/引号，允许换行继续输入 

F13 : 优雅降级 -> 错误处理, 标题更明确 
强调失败类型: （鉴权失败、限流、网络中断、模型不存在等）
强调 程序不退出
强调 错误信息的样式要不同 
-->
```

## 非功能需求

- N1: **界面不阻塞**
  网络请求与界面渲染互不阻塞。流式数据以异步方式驱动界面更新，等待与流式期间界面
  始终保持响应（可滚动、可见进行中指示），不得冻结。
- N2: **流式实时性与等待反馈**
  在收到首个文本增量前，显示带动画与实时秒数的"处理中"指示（如 "Imagining… (5s)"），
  让用户明确知道正在等待模型；文本增量到达后尽快逐字呈现，给用户"实时打字"的观感。
- N3: **跨协议一致体验**
  无论使用 Anthropic 还是 OpenAI 协议，用户感知的输入、流式输出、多轮上下文与错误
  反馈行为保持一致。
- N4: **配置健壮性**
  配置文件缺失、YAML 格式错误、必要字段缺失等情况，给出明确可读的错误提示，而非
  崩溃堆栈。
- N5: **密钥安全**
  API 密钥不在界面回显，不打印到对话区或任何日志输出中。
- N6: **终端兼容与自适应**
  在常见终端下正常显示；markdown 渲染与界面布局对终端宽度自适应，窄屏不错版。
- N7: **退出整洁**
  退出时恢复终端状态（清理 raw mode 等），不残留损坏的终端环境。



```

 <!-- @ignore
非功能需求完全落败, 记住添加的这些点

N1 放在非功能需求中根本不合理, 直接去除
**Python 3.10+** — 依赖 `prompt_toolkit`（TUI）、`rich`（Markdown 渲染）、`anthropic`（Anthropic SDK）、`openai`（OpenAI SDK）、`pyyaml`（配置解析）

其他项模板的比自己生成的更完善, 直接全部替换
- N2: **配置文件路径** — 位于 `~/.newcode/config.yaml`，不存在时提示用户创建并提供模板
- N3: **API Key 安全** — 支持 `${ENV_VAR}` 语法从环境变量读取，也可直接填写
- N4: **流式输出到 stdout** — 正常输出到 stdout，错误信息到 stderr

-->
```



## 不做的事

- 工具调用（Tool Use / Function Calling）
- 文件读写 / Shell 命令执行
- 对话历史持久化（退出即丢失）
- MCP / 插件系统
- 多会话管理
- 上下文智能压缩（仅简单滑动窗口）, 更完善的留给后面章节
- 自动补全 / 历史命令搜索
- 会话导出 / 导入
- 权限系统：无任何需授权的操作，故不做权限确认。
- 长期记忆 / 跨会话记忆：不做
- slash 命令体系：除 /exit 外，不做可扩展的命令系统（如 /help、/clear、/model 等）
- 运行时切换 provider / model：多份配置仅在启动时选一次，会话中途不切换
- thinking 内容展示：扩展思考增量接收即丢弃，不渲染、不折叠展示
- 其它配置来源：仅读 YAML 配置文件，不做环境变量 / 命令行 flag 覆盖



```
 <!-- @ignore

漏了的
权限系统：无任何需授权的操作，故不做权限确认。
长期记忆 / 跨会话记忆：不做
slash 命令体系：除 /exit 外，不做可扩展的命令系统（如 /help、/clear、/model 等）
运行时切换 provider / model：多份配置仅在启动时选一次，会话中途不切换
其它配置来源：仅读 YAML 配置文件，不做环境变量 / 命令行 flag 覆盖
用量统计：不显示 token 数与费用（但保留响应耗时计时，见 F16）。
多模态：不支持图片等非文本输入。

thinking 内容展示：扩展思考增量接收即丢弃，不渲染、不折叠展示 
这个前面的需求有提到过, 再强调一遍也没有问题 

样例不打算做, 按时我不想加的, 直接加到需求中去
流式中断：本期不支持取消正在进行的回复
自动重试 / 限流退避：出错仅提示，不自动重试。

原本有我取消了的
TUI 内代码高亮 / 语法着色

-->
```



## 验收标准

- AC1: 启动 `newcode`，终端显示完整界面布局：顶部 ASCII 小狗横幅 + 应用名与版本号 + 当前工作目录，底部带边框的输入框含 `❯`
  提示符与占位文字，底部状态栏显示活动 provider 名称和模型名
  - AC2: `newcode -c "你好"` 输出回复后立即退出，不进入 TUI

  - AC3: 输入问题后，AI 回复逐 token 实时出现在对话区，用户无需等待即可看到内容生成过程

  - AC4: 配置 `thinking: true` 的 Anthropic provider 回复复杂问题时，thinking 增量被正确丢弃，对话区中不出现思考内容，正文完整无混入

  - AC5: 对话超过配置的 `max_turns` 轮后，AI 不再引用最早的消息

  - AC6: 在输入框中按 `Alt+Enter` 插入换行，输入框内出现多行文本；按 `Enter` 提交后，输入框清空并进入等待状态，期间不接受新的输入，直至本轮回复结束

  - AC7: `/exit`、`/quit`、`Ctrl+C`、`Ctrl+D` 四种方式均能正常退出，退出后终端状态恢复正常

  - AC8: 配置文件中设置 `system_prompt: "请用 JSON 格式回复"` 后，AI 回复为合法 JSON

  - AC9: `~/.newcode/config.yaml` 不存在时，提示用户创建配置文件并退出

  - AC10: 修改 `provider` 字段从 `anthropic` 协议切到 `openai` 协议后，调用不同 API 后端，对话体验保持一致

  - AC11: 新增一个 provider 类型只需实现统一接口，无需修改核心逻辑

  - AC12: 代码块、加粗、列表等 Markdown 语法在终端中正确渲染；本轮回复结束后，整段回复以 Markdown 形式重新渲染美化后定型展示

  - AC13: 使用无效 API key 发起请求时，对话区以可区分的样式（如红色或不同前缀）显示错误信息，程序不退出，对话历史保留，用户可继续输入重试

  - AC14: 每轮对话请求发出时，状态栏或对话区显示实时计时指示（如 "Imagining… (5s)"），秒数随时间递增，回复结束后定型显示该轮总耗时

  - AC15: AI 流式回复进行中时，按 `ESC` 键取消当前回复，对话区显示取消提示，恢复输入状态，对话历史保留

  - AC16: API 调用失败时自动重试 3 次，每次间隔 3 秒，界面中以可区分的样式展示重试次数和状态

```
 <!-- @ignore
 修改spec后, 验收标准也需要同步调整 , 下面是有问题的地方 和 旧版本的标准 
 
  问题 1：AC5 和 F5 矛盾                                                                                         AC5 写的是 自动检测未闭合括号/引号，但 F5 已经改成了 Alt+Enter 手动换行。AC5 需要重写。

  问题 2：AC11 描述有误
AC11 写 回复包含 thinking 内容，但 F3 和 F11 都说了 thinking 增量接收即丢弃、不渲染。AC11 应该验证 thinking 功能已启用（比如回复质量更高），而非"包含
  thinking 内容"。

  问题 3：缺少覆盖
新增的 F14、F15、F16、F17 和自动重试都没有对应的验收标准。AC1 也没覆盖 F15 的布局细节和 F1 的"回复结束后 markdown 重新渲染"。

  问题 4：AC13 不完整
F13 强调"以可区分的样式展示错误"，AC13 没提。

 
- AC1: 启动 `newcode`，看到 `> ` 提示符，输入问题后收到流式逐字回复
- AC2: `newcode -c "你好"` 输出回复后立即退出，不进入 TUI
- AC3: AI 回复逐 token 实时出现，用户在生成过程中即可看到内容
- AC4: 对话超过配置的 `max_turns` 轮后，AI 不再引用最早的消息
- AC5: 输入 `print("hello` 后按回车，自动换行等待补全，输入 `")` 关闭括号后按回车才发送
- AC6: `/exit`、`/quit`、`Ctrl+C`、`Ctrl+D` 四种方式均能正常退出
- AC7: 配置文件中设置 `system_prompt: "请用 JSON 格式回复"` 后，AI 回复为合法 JSON
- AC8: `~/.newcode/config.yaml` 不存在时，提示用户创建配置文件并退出
- AC9: 修改 `provider` 字段从 `anthropic` 协议切到 `openai` 协议后，调用不同 API 后端
- AC10: 新增一个 provider 类型只需实现统一接口，无需修改核心逻辑
- AC11: Anthropic provider 配置 `thinking: true` 时，回复包含 thinking 内容
- AC12: 代码块、加粗、列表等 Markdown 语法在终端中正确渲染
- AC13: 断网时调用 API，显示错误信息，对话历史保留，用户可继续输入重试
 
 -->
```

