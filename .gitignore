# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.venv/
venv/
.env

# NewCode config (contains real API keys)
.newcode.yaml

# NewCode 权限本地规则（个人本地，不随仓库分发）
.newcode/permissions.local.yaml
.newcode/permissions.yaml

# NewCode Hook 本地配置（个人本地，不随仓库分发，ch12 F6.1）
.newcode/config.local.yaml

# NewCode 上下文管理落盘目录（会话级，运行时数据，不入库）
.newcode/sessions/

# NewCode 本地监控数据（运行时写入，不入库）
.newcode/monitor/

# Plan files (runtime data in project root, NOT newcode/plans code package)
/plans/

# CC Switch docs (downloaded reference files)
ccswitch_*.html
ccswitch_*.md
ccswitch_*.txt

# Backups
CLAUDE(旧的).md.bak
docs_bak/

# IDE
.vscode/
.idea/

# Claude local settings
.claude/settings.local.json
# NewCode 本地技能（个人配置，不入库）
.newcode/skills/

# NewCode 本地 Agent 定义（个人配置，不入库）
.newcode/agents/

# Scratch / test output
/tmp/

# uv lockfile（本仓库沿用 pip/venv 工作流，锁文件不入库）
uv.lock

# ch14: Worktree 隔离副本（运行时数据，不入库）
.newcode/worktrees/
.newcode/worktree_session.json
